# Benchmark output stays local: run directories, monitoring logs, ad-hoc
# snapshots. The published numbers live in RESULTS.md, not here.
# Commit a run deliberately with `git add -f` if you want to publish it.
results/*
!results/.gitkeep
